require declaration

constructor Type(line, id, fields)

    :show
        "<Type "+id+" " + fields + ">"
    end

end

:parse_type(json)
    Type(json["line"], json["id"], json["fields"].map!(parse_declaration))
end