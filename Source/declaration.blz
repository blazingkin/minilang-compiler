require syntax_exception
require types/primitive_type
require types/type_factory

constructor Declaration(line, id, typ)

    :show
        "<Declaration "+id + " " + typ + ">"
    end

    :== (other)
        return other.id == id && other.typ == typ
    end

end

:parse_declaration(json)
    Declaration(json["line"], json["id"], parse_type(json["type"]))
end