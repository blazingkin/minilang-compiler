require syntax_exception

constructor Declaration(line, id, type)

    :show
        "<Declaration "+id + " " + type + ">"
    end

end

:parse_declaration(json)
    Declaration(json["line"], json["id"], json["type"])
end