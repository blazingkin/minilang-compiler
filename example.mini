# Milestone 1: General Functionality and Optimizations Benchmark

struct IntHolder{
   int num;
};

struct IntIntHolder {
   struct IntHolder f;
};

struct IntIntIntHolder {
   struct IntIntHolder f;
};
struct IntIntIntIntHolder {
   struct IntIntIntHolder f;
};

fun main() int {
   struct IntIntIntIntHolder x;
   int num;
   if (x == null){
      
   }
   x.f.f.f.num = x.f.f.f.num;
   return num;
}